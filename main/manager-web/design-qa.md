# TongDou 登录 / 注册页面设计验收

## 对比基准

- 正式素材：`E:\ESP32-S3\TongDou_Release_Preparation\TongDou_Assembly.png`
- 实现页面：`#/login`、`#/register`
- 桌面检查尺寸：1024×768、1280×800、1440×900
- 手机检查尺寸：320×568、390×844、430×932、768×1024
- 对比图：`C:\Users\Administrator\.codex\visualizations\2026\08\26\01a03b98-6a1b-7602-b878-d2b7bd06649e\tongdou-auth\qa-source-vs-login.png`
- 局部状态证据：密码闭眼、登录失败、登录成功截图

## 迭代记录

### 第一轮

- P1：手机端原布局存在横向空间风险。已改为 800 像素以下上下排列，并在 320、390、430、768 像素实际检查，无横向滚动。
- P2：桌面左右比例偏向模型，表单显得过窄。已调整为约 55% / 45%，保持铜豆主体明确，同时提高表单可读性。
- P2：768 像素附近受浏览器小数视口影响，可能误入桌面布局。断点调整到 800 像素后复测通过。
- P2：启动眼睛顺序不够清楚。已整理为熄屏、亮眼、眨眼、显示台词；系统开启“减少动态效果”时直接显示静态完成态。

### 最终复核

- 正式透明 PNG 未改动，只复制到项目资源目录。
- OLED 眼睛覆盖在实机屏幕范围内，桌面和手机端均保持对位。
- 登录、注册共用同一个铜豆视觉组件；表单业务逻辑仍留在各自页面。
- 密码输入闭眼、账号输入注视、失败警觉、成功放行状态均已检查。
- 登录、注册、找回密码页面均无新增控制台错误。
- 生产构建通过；仅保留项目已有的大资源体积警告，以及新增正式 PNG 的体积警告。

## 未阻断项

- 正式 PNG 约 1.58 MiB，会增加登录页首次加载体积；为保证首版不改原图画质，本轮不压缩。
- 当前检查浏览器开启了“减少动态效果”，所以实际页面按设计关闭连续眨眼、鼠标跟随和逐字动画；对应代码路径已通过构建和静态检查。

## 左侧主视觉融合优化

- 视觉基准：`login-before-left-polish-1440.png`。
- 登录实现截图：`login-left-polish-1440-final.png`。
- 注册实现截图：`register-left-polish-1440-final.png`。
- 对比尺寸：源图与实现图均为 1440×900 像素；CSS 视口 1440×900，设备像素比约 1.22，截图已由浏览器归一化到 CSS 尺寸。
- 对比状态：中文、深色主题、未输入、默认闲置眼睛。
- 全景对比：`qa-left-visual-before-after.png`。铜豆显示宽度由约 708 像素缩至约 580 像素，缩小约 18%；角色整体稍向左、向下，台词移到角色下方，右侧表单位置和尺寸未变化。
- OLED 局部对比：`qa-oled-glow-before-after.png`。眼睛本体颜色保持青蓝色，外发光由 3 像素高透明度阴影降为 1 像素低透明度阴影，更接近实体 OLED。
- 颜色与图片：正式 PNG 未改动；显示层使用 `brightness(0.92)`、`saturate(0.88)`，没有出现过暗、灰雾或透明边缘问题。
- 环境融合：机器背后增加低透明度径向环境层，底部增加柔和椭圆阴影；对比中没有出现明显光晕或悬浮感。
- 字体与文案：未修改字体、字号、台词内容和右侧表单文案。
- 交互复核：密码聚焦后眼睛高度仍为 `2.2`、位置仍为 `49`，闭眼和台词逻辑正常；浏览器控制台新增错误为 0。
- 迭代结论：第一轮已达到目标，没有遗留 P0、P1、P2 问题；本轮未继续增加装饰性效果。

## 左侧环境融合专项复核

- 视觉基准：`login-env-before-1440.png`。
- 最终实现：`login-env-polish-1440-final.png`。
- 全景对比：`qa-env-before-after-final.png`。
- 局部对比：`qa-env-details-before-after-final.png`，重点检查 OLED、材质明暗和机器底部阴影。
- 对比条件：源图和实现图均为 1440×900 像素；CSS 视口 1440×900，默认中文登录状态。
- 尺寸与位置锁定：修改前后图片容器均为约 580.24×516.83 像素，坐标均为约 `(82.67, 153.08)`，变换矩阵保持 `translate(-29.01px, 34px)`；台词坐标和宽高也保持一致。
- 材质：显示滤镜由较灰暗的 `brightness(0.92) saturate(0.88)` 调整为 `brightness(0.95) saturate(0.96) contrast(1.04)`，并移除包围整机的 `drop-shadow`。
- 环境：左侧使用无边界、低透明度的冷蓝灰径向环境层和极弱地面层，没有出现矩形面板边界或明显光晕。
- 落地阴影：机器底部使用窄而深的接触阴影，以及宽而低透明度的环境阴影；局部对比中阴影保持克制，没有形成黑色硬边。
- OLED：眼睛仍为青蓝色矩形，仅保留 1 像素低透明度光感；屏幕层使用极弱青色环境反射，没有霓虹外发光。
- 字体、间距与文案：右侧表单、台词、字号、排版均未修改。
- 浏览器检查：页面宽度 1440 像素，无横向滚动，新增控制台错误为 0。
- 结论：没有遗留 P0、P1、P2 问题；环境融合效果符合“可感知但不显眼”的目标。

## 右侧认证区域融合复核

- 视觉基准：`login-card-before-1440.png`。
- 登录实现：`login-card-fusion-1440-final.png`。
- 注册实现：`register-card-fusion-1440-final.png`。
- 全景对比：`qa-card-before-after-final.png`。
- 表单局部对比：`qa-card-focus-before-after-final.png`。
- 对比条件：源图和实现图均为 1440×900 像素；中文、默认未输入状态。
- 布局锁定：登录容器修改前后均为约 519.99×605.51 像素，坐标均为约 `(842.69, 122.23)`；表单内边距、输入框、按钮和文字位置没有变化。
- 容器融合：可见边框改为透明占位边框，保持盒模型尺寸；22 像素大圆角改为 0，移除 70 像素悬浮阴影和 16 像素背景模糊。
- 背景：容器改为边缘完全透明的低透明度径向背景，只在表单中心提供轻微层次，没有形成完整矩形轮廓。
- 输入与状态：输入框自身边框、金色主按钮、青色状态文字和当前排版保持不变。
- 左侧锁定：铜豆容器仍为约 580.24×516.83 像素，坐标和变换矩阵与修改前一致；`TongDouAuthVisual.vue` 未在本轮修改。
- 功能检查：登录与注册页面均正常渲染，验证码图片正常加载，无横向滚动，新增控制台错误为 0。
- 结论：没有遗留 P0、P1、P2 问题；表单已从独立悬浮卡片转为同一控制中心空间内的功能区域。

final result: passed
